WPGRAPHQL_ENDPOINT=https://example.com/graphql
SITE_URL=http://localhost:3000
# เมื่อ WP timeout/เชื่อมต่อไม่ได้: 1=ใช้ fallback (แสดงหน้าเปล่า), 0=throw error. ใน dev เปิด fallback โดยอัตโนมัติ
# WP_FALLBACK_ON_ERROR=1
# เมื่อ WP ตอบ 500/502 ตอน build: ใช้ slugs เหล่านี้ pre-generate หน้า (comma-separated). Locations ใช้ data/locations อัตโนมัติ
# FALLBACK_CATEGORY_SLUGS=mobile,notebook,tablet
# FALLBACK_PRICE_SLUGS=macbook-air-m2-2023,iphone-15-pro-128gb
# FALLBACK_SERVICE_SLUGS=buy-macbook,buy-iphone
# คีย์ลับสำหรับเรียก On-Demand Revalidate (เมื่อ WordPress อัปเดตเนื้อหา). ใช้ใน Authorization: Bearer
# REVALIDATE_SECRET=your-secret-key-here

# จำกัดจำนวนหน้าที่ pre-build (ใส่ 0 หรือไม่ตั้ง = pre-build ทั้งหมด). หน้าที่เกิน limit จะสร้าง on-demand แล้ว ISR ทุก 30 นาที
# BUILD_MAX_LOCATION_PAGES=0
# BUILD_MAX_SERVICE_PAGES=0
# BUILD_MAX_PRICE_PAGES=0